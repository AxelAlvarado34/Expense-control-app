export type CatoriesType = {
    id: string,
    name: string,
    icon: string
}

export type ExpenseType = {
    id: string
    expenseName: string,
    amount: number,
    category: string,
    date: string,
}